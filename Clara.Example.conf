###########################
#-->   Configuration  <-- #
###########################

set Clara(ip)				"ton-ip"
set Clara(info)				"Clara Service"
set Clara(link)				"Clara.eggdrop.fr"
set Clara(port)				"6900"
set Clara(pass)				"ton-pass"

#############################
#--> Configuration Clara <--#
#############################

# Pseudo du Service
set Clara(pseudo)			"Clara"

# RealName du Service
set Clara(real)				"40 F"

# Ident du Service
set Clara(ident)			"Clara"

# Host du Service
set Clara(host)				"Clara.eggdrop.fr"

# Chanlog du Service
set Clara(salon)			"#Services"

# Mode du Service
set Clara(mode)				"+ABOS"

# Mode du salon de logs.
set Clara(cmode)			"Osnt"

# Activation de la console (1)oui (0)non
set Clara(console)			1

######################################
#--> Configuration Administration <--#
######################################

#Mettez ci-dessous l'url de votre site
set Clara(site)				"https://www.ton-nom.fr"

# Version du Service
set Clara(version)			"1.1"

# Codeurs du Service
set Clara(auteur)			"MalaGaM"

# Equipe de votre r�seau
set Clara(equipe)			[list "Claire"]

#Mettez ci-dessous votre pseudo
set Clara(Admin_pseudo)		[list "Claire"]

#Mettez ci-dessous votre pass
set Clara(Admin_password)	"votre-mot-2-pass"

# Activez le mode debug socket?
set Clara(debug)			0

# Ancien protocol irc = 0, nouveau = 1
set Clara(protocolvers)		1

# Identification serveur (ne pas toucher logiquement)
set Clara(SID)				"00C"